name: Build and Release

on:
  push:
    branches: [ main ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write
  packages: write

env:
  SERVER_URL: ${{ secrets.SERVER_URL || 'https://your-app.ondigitalocean.app' }}
  CLIENT_SECRET: ${{ secrets.CLIENT_SECRET }}

jobs:
  deploy-server:
    runs-on: ubuntu-latest
    steps:
    - name: App Platform deployment info
      run: |
        echo "🌊 DigitalOcean App Platform Deployment"
        echo "======================================="
        echo ""
        echo "ℹ️  App Platform automatically deploys your server from this repository"
        echo "📂 Server code: ./cmd/server/"
        echo "⚙️  Configuration: ./.do/app.yaml"
        echo ""
        
        if [ -z "${{ secrets.SERVER_URL }}" ]; then
          echo "⚠️  DIGITALOCEAN_APP_URL secret is not set!"
          echo "📝 Please set it to your App Platform app URL (e.g., https://your-app.ondigitalocean.app)"
          exit 1
        fi
        
        echo "✅ Using DigitalOcean App Platform deployment"
        echo "📝 App Platform will automatically deploy from your repository"
        echo "🔧 Make sure to set CLIENT_SECRET in your App Platform environment variables"
        echo "🌐 Your app URL should be set as DIGITALOCEAN_APP_URL secret"
        echo ""
        echo "📋 Setup checklist:"
        echo "1. ✅ Create app on DigitalOcean App Platform"
        echo "2. ✅ Connect your GitHub repository"
        echo "3. ✅ Set CLIENT_SECRET environment variable in App Platform"
        echo "4. ✅ Set DIGITALOCEAN_APP_URL secret in GitHub"
        echo "5. ✅ App Platform will auto-deploy on every push to main"
        echo ""
        echo "📖 For detailed setup: See DIGITALOCEAN_APP_PLATFORM.md"
        echo "🌐 App Platform handles server deployment automatically!"
        echo "🎯 This workflow only builds client binaries with your App Platform URL"

  build-clients:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        goos: [windows, linux, darwin]
        goarch: [amd64, arm64]
        exclude:
          - goos: windows
            goarch: arm64

    steps:
    - uses: actions/checkout@v4
    
    - name: Workflow info
      run: |
        echo "🏗️  Building Planning Poker CLI client"
        echo "OS: ${{ matrix.goos }}"
        echo "Arch: ${{ matrix.goarch }}"
        echo "Server URL: ${{ env.SERVER_URL }}"
        if [ "${{ startsWith(github.ref, 'refs/tags/') }}" = "true" ]; then
          echo "Building release version with CLIENT_SECRET"
        else
          echo "Building development version"
        fi
    
    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.23'
    
    - name: Get dependencies
      run: go mod download
    
    - name: Validate secrets
      run: |
        if [ "${{ startsWith(github.ref, 'refs/tags/') }}" = "true" ]; then
          if [ -z "${{ env.CLIENT_SECRET }}" ]; then
            echo "Error: CLIENT_SECRET is required for tag builds"
            exit 1
          fi
        else
          echo "Non-tag build: CLIENT_SECRET validation skipped"
        fi
    
    - name: Build client with embedded config
      env:
        GOOS: ${{ matrix.goos }}
        GOARCH: ${{ matrix.goarch }}
        CGO_ENABLED: 0
        CLIENT_SECRET: ${{ env.CLIENT_SECRET }}
        SERVER_URL: ${{ env.SERVER_URL }}
      run: |
        # Get version info
        if [ "${{ startsWith(github.ref, 'refs/tags/') }}" = "true" ]; then
          VERSION=${GITHUB_REF#refs/tags/}
        else
          VERSION="dev-${{ github.sha }}"
        fi
        
        echo "Building version: $VERSION"
        echo "Target: $GOOS/$GOARCH"
        echo "Server URL: $SERVER_URL"
        
        # Create build-time configuration 
        LDFLAGS="-s -w -X main.BuildServerURL=${SERVER_URL} -X main.BuildClientSecret=${CLIENT_SECRET} -X main.BuildVersion=${VERSION} -X main.BuildCommit=${COMMIT} -X main.BuildDate=${DATE}"
        
        echo "🔧 Building with configuration:"
        echo "  Version: ${VERSION}"
        echo "  Server URL: ${SERVER_URL}"
        echo "  Client Secret: $(echo ${CLIENT_SECRET} | cut -c1-8)..."
        echo "  Platform: ${{ matrix.goos }}/${{ matrix.goarch }}"
        echo "  LDFLAGS: ${LDFLAGS}"
        
        echo "🏗️  Starting Go build..."
        if [ "${{ matrix.goos }}" = "windows" ]; then
          go build -ldflags="${LDFLAGS}" -o planning_poker_secure_${{ matrix.goos }}_${{ matrix.goarch }}.exe ./cmd/client
          echo "✅ Built: planning_poker_secure_${{ matrix.goos }}_${{ matrix.goarch }}.exe"
        else
          go build -ldflags="${LDFLAGS}" -o planning_poker_secure_${{ matrix.goos }}_${{ matrix.goarch }} ./cmd/client
          echo "✅ Built: planning_poker_secure_${{ matrix.goos }}_${{ matrix.goarch }}"
        fi
        
        # Verify the binary was created
        if [ "${{ matrix.goos }}" = "windows" ]; then
          ls -la planning_poker_secure_${{ matrix.goos }}_${{ matrix.goarch }}.exe
        else
          ls -la planning_poker_secure_${{ matrix.goos }}_${{ matrix.goarch }}
        fi
    
    - name: Upload client artifacts
      uses: actions/upload-artifact@v4
      with:
        name: client-${{ matrix.goos }}-${{ matrix.goarch }}
        path: planning_poker_secure_${{ matrix.goos }}_${{ matrix.goarch }}*
        retention-days: 30

  release:
    if: startsWith(github.ref, 'refs/tags/')
    needs: build-clients
    runs-on: ubuntu-latest
    
    steps:
    - name: Download all artifacts
      uses: actions/download-artifact@v4
      with:
        merge-multiple: true
    
    - name: Display downloaded files
      run: ls -la
    
    - name: Create Release
      uses: softprops/action-gh-release@v2
      with:
        files: |
          planning_poker_secure_*
        draft: false
        prerelease: ${{ contains(github.ref, '-') }}
        generate_release_notes: true
        body: |
          ## Planning Poker CLI Release
          
          ### 🚀 DigitalOcean App Platform Deployment
          
          This release includes pre-configured clients for DigitalOcean App Platform deployment.
          
          ### 📦 Downloads
          
          Choose the appropriate binary for your platform:
          
          - **Windows (64-bit)**: `cli_planning_poker_windows_amd64.exe`
          - **Linux (64-bit)**: `cli_planning_poker_linux_amd64`
          - **Linux (ARM64)**: `cli_planning_poker_linux_arm64`
          - **macOS (Intel)**: `cli_planning_poker_darwin_amd64`
          - **macOS (Apple Silicon)**: `cli_planning_poker_darwin_arm64`
          
          ### ⚙️ Configuration
          
          These binaries are pre-configured with:
          - **Server URL**: ${{ env.SERVER_URL }}
          - **Authentication**: Embedded client secret (secure)
          
          ### 🏃‍♂️ Usage
          
          1. Download the appropriate binary for your platform
          2. Make it executable (Linux/macOS): `chmod +x cli_planning_poker_*`
          3. Run: `./cli_planning_poker_*` (or double-click on Windows)
          
          ### 📖 Documentation
          
          - [Setup Guide](DIGITALOCEAN_APP_PLATFORM.md)
          - [Migration from Vercel](MIGRATION_GUIDE.md)
          
          The server is automatically deployed to DigitalOcean App Platform from this repository.
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}